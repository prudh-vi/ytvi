{
  "name": "ytvi",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/express": "^5.0.1",
    "express": "^5.1.0",
    "nodemon": "^3.1.9"
  },
  "scripts": {
    "dev": "bun run --watch index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}